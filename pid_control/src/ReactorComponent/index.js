import React from 'react'


const ReactorComponent = (props) =>{

    const {model} = props

    return (
        <React.Fragment>
            REACTOR
        </React.Fragment>
    )
}

export default ReactorComponent